.env
deploy/.env
*.log
logs/*
build/
*.prof
coverage.*
.air.toml
.idea/
.vscode/
.DS_Store

# Compiled binaries (anchored to repo root — the bare patterns "evolution-go"
# and "server" also matched cmd/evolution-go/ and pkg/server/, which is why the
# original fork was published without those directories and didn't build)
/evolution-go
/server

# Frontend (built in Docker)
manager/dist/
evolution-go-manager/node_modules/
evolution-go-manager/dist/
.github/workflows/
